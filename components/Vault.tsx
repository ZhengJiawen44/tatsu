import React from "react";
import { cn } from "@/lib/utils";
const Vault = ({ className }: { className?: string }) => {
  return <div className={cn("", className)}>Vault</div>;
};

export default Vault;
